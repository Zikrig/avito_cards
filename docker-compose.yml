services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./config.json:/app/config.json
      - ./output:/app/output
      # Примеры (3 фото по file_id, логотип, тексты) — чтобы не терялись после пересборки
      - ./data:/app/data
    restart: unless-stopped
